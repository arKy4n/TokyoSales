class User {
  constructor(userId, username, password, email) {
    this.userId = userId;
    this.username = username;
    this.password = password;
    this.email = email;
  }
}

module.exports = User;
